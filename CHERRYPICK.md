# Cherry-Pick

`7797fa5` on `lab09-feature` and `d5f88e2` on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.
